# NETCDF and Go

## Notes

```TEXT
dimensions(sizes): feature_id(2729077), time(1), reference_time(1)
variables(dimensions): int32 time(time), int32 reference_time(reference_time), |S1 crs(),
int32 feature_id(feature_id), int32 streamflow(feature_id), int32 nudge(feature_id), int32 velocity(feature_id),
int32 qSfcLatRunoff(feature_id), int32 qBucket(feature_id), int32 qBtmVertRunoff(feature_id)
```
